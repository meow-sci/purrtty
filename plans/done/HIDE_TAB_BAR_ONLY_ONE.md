When there's only one session active dont paint the tab bar.

This must change the window height calculation and snap behavior to account for when the tab bar is painted and not since it's part of the window height calculation.