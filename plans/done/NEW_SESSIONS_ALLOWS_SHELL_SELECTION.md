change tab bar + button to be a menu with each available shell to pick from instead of only using the default

change file new session menu to have a submenu to pick the shell to start

default shell option remains so initial terminal session uses it
