name = "caTTY"
description = "A terminal emulator for KSA with full VT100/xterm compatibility"
version = "1.0.0"
author = "caTTY Project"

# Once https://github.com/StarMapLoader/StarMap/tree/feature/mod-dependencies/StarMap.API
# (the mod-dependencies) branch is merged, we can re-enable this.
# [StarMap]
# EntryAssembly = "caTTY.GameMod"
